def is_sorted(arr):
    n = len(arr)
    for i in range(n - 1):
        if arr[i] > arr[i + 1]:
            return "false"
    return "true"

# Read input
n = int(input())
arr = list(map(int, input().split()))

# Check if the array is sorted and print the result
print(is_sorted(arr))
