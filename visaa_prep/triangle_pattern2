# Read the height of the triangle
N = int(input().strip())

# Loop through each level of the triangle
for i in range(1, N + 1):
    # Print the number i, i times
    print((str(i) + ' ') * i)  # This will print i followed by a space, i times
